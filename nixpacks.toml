# Nixpacks build config for Railway.

[phases.setup]
nixPkgs = ["nodejs_22"]

[phases.install]
# --include=dev forces devDependencies (typescript, vite, ...) to install
# even if Railway has NODE_ENV=production in the service variables.
cmds = ["npm ci --include=dev"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"
